# Runtime mode only. Never add recovery phrases, private keys, WIF values, or
# upstream provider tokens used outside local development.
CIPHER_WALLET_ENVIRONMENT=local

# The gateway remains unauthenticated in the Testnet MVP. Provider settings are
# added only when the provider decision issue is complete.
CIPHER_WALLET_CHAIN_PROVIDER_URL=
